apiVersion: 1

providers:
  - name: 'CloudScale Dashboards'
    orgId: 1
    folder: 'CloudScale'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/dashboards
